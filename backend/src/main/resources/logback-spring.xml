<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 库存调整日志文件输出 -->
    <appender name="INVENTORY_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 日志文件路径，相对于项目根目录 -->
        <file>logs/inventory-adjust.log</file>
        <!-- 滚动策略：每天生成一个新文件 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/inventory-adjust.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory> <!-- 保留30天日志 -->
        </rollingPolicy>
        <!-- 日志格式 -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
    
    <!-- 库存调整日志单独配置 -->
    <logger name="com.scm.controller.InventoryController" level="INFO" additivity="true">
        <appender-ref ref="INVENTORY_FILE"/> <!-- 输出到库存调整日志文件 -->
    </logger>
</configuration>